// Define the props interface for the Text component
export interface TextProps {
  disabled?: boolean; // Optional prop to indicate if the text is disabled
  children: React.ReactNode; // The content to be displayed inside the Text component
} 